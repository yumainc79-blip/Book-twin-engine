[project]
name = "book-twin-engine"
version = "0.1.0"
description = "AI-native book repository engine"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
